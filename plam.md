## Plan: Завершение по ТЗ и исправление фронтенда

TL;DR - Что и почему:
Проанализировал ТЗ и кодовую базу. Основные проблемные места: незавершённые пункты ТЗ (Alembic, Swagger, Donut chart, часть UI-функций, тесты, CI) и баг UI на главной странице: частицы перекрывают контент / ведут себя не как фон; отсутствует кнопка переключения темы в UI. Рекомендую сфокусироваться на приоритетах для защиты: 1) миграции (Alembic), 2) Swagger / API docs, 3) темы + исправление частиц и добавление кнопки переключения темы, 4) визуализация (donut chart), 5) несколько тестов и базовый GitHub Actions. План делится на фазы: подготовка, фронтенд, бэкенд, верификация.

**Steps**
1. Подготовка (проверить окружение) — локально собрать зависимости, убедиться в возможности запускать backend и frontend (*depends on: доступ к окружению пользователя*).
2. Анализ и приоритизация (выполнено) — просмотр ТЗ и уже реализованных пунктов.
3. Фронтенд — исправить поведение частиц и сделать их настоящим фоном: перенести элемент `canvas#particles-canvas` в бекграунд, настроить CSS слоя (z-index), сделать ресайз и pointer-events:none; добавить кнопку переключения темы в навигацию и связать с `theme.js`; обеспечить сохранение темы в localStorage и серверное сохранение через `/api/v1/profile/theme` при авторизованном юзере; исправить видимость текста (padding/margins) чтобы частицы не выталкивали контент; обеспечить корректный переход тем (CSS-переменные + transition 300ms).
4. Фронтенд — улучшения UI: исправить, чтобы анимации карточек были фоновые (stagger on content load), добавить theme switcher и preview выбранной темы; улучшить адаптивность на малых экранах (проверить breakpoints 320/768/1024).
5. Фронтенд — добавить Donut chart на страницу результата (Chart.js) вместо простых bar fills; добавить typewriter уже есть — проверить и привести в соответствие; добавить кнопку переключения темы видимой в `.nav`.
6. Бэкенд — подключить Alembic (инициализация и базовая миграция): добавить alembic.ini и папку `migrations`, простая первая миграция для users/searches; обновить `backend/requirements.txt` и `docker-compose.yml` при необходимости.
7. Бэкенд — Swagger via `flask-smorest` или `flasgger`: добавить базовую автодокументацию на `/api/docs` и описать ключевые эндпоинты (`/auth/*`, `/analyze`, `/health`, `/profile/*`).
8. Бэкенд — небольшие фиксы: endpoint `/profile/theme` уже есть — связать фронтенд; проверить, что rate limiting соответствует ТЗ (10/час) — поправить, если нужно.
9. Тесты и CI — написать 5-6 реальных pytest для `/auth/register`, `/auth/login`, `/health` и пару unit-тестов для scorer/embeddings; добавить простой GitHub Actions workflow запускающий `pytest` на push в main.
10. Документация и сборка — обновить README скриншотами/инструкциями для запуска, добавить `.env.example` (поправить, если нужно) и инструкции миграций.

**Relevant files**
- [ТЗ](ТЗ#L1-L157) — техническое задание (источник правок и приоритетов)
- [README.md](README.md) — инструкции по запуску
- [frontend/index.html](frontend/index.html) — главная страница, canvas частиц
- [frontend/css/style.css](frontend/css/style.css) — тема/переменные/анимации
- [frontend/js/particles.js](frontend/js/particles.js) — логика частиц
- [frontend/js/theme.js](frontend/js/theme.js) — текущая тема (save в localStorage)
- [frontend/js/app.js](frontend/js/app.js) — UI логика анализа
- [backend/app.py](backend/app.py) и [backend/app/__init__.py](backend/app/__init__.py) — запуск и создание приложения
- [backend/app/routes.py](backend/app/routes.py) — API эндпоинты
- [backend/tests/test_pipeline.py](backend/tests/test_pipeline.py) — существующие тесты, расширить

**Verification**
1. Локальный запуск: `docker-compose up --build` (или локальный запуск сервера) — фронтенд доступен по `http://localhost:8080`, бэкенд `http://localhost:5000`.
2. UI: на главной странице частицы работают как фон (не мешают прокрутке), текст полностью виден, кнопка переключения темы в навигации, тема меняется и сохраняется в localStorage и на сервере (если пользователь залогинен).
3. Endpoints: `/api/v1/profile/theme` работает (POST), `/api/v1/health` возвращает статус сервисов.
4. Тесты: `pytest` — минимум 70% покрытия для изменённых модулей (минимально 5-6 тестов).
5. Документация: `/api/docs` доступна и содержит основные эндпоинты.

**Decisions / Предположения**
- Приоритет работ: фокус на том, что наиболее заметно на защите (миграции, Swagger, визуальная часть и несколько тестов).
- Тема: реализую 5 предустановленных тем из ТЗ с простыми палитрами; если хотите другие цвета — пришлите палитры.
- PDF экспорт уже работает через `app.export.pdf_export` — оставляю как есть, проверю работоспособность.

**Further Considerations**
1. Хотите ли вы, чтобы я реализовал все оставшиеся пункты ТЗ сразу, или ограничиться приоритетным списком для защиты? Рекомендую начать с приоритетов и при времени расширять функционал.