# Database
# DATABASE_URL=sqlite:///searches.db
# DATABASE_URL=postgresql+psycopg2://webanalyzer:secret@localhost:5432/webanalyzer

# Redis (optional — for Celery + caching)
# REDIS_URL=redis://localhost:6379/0

# Auth — CHANGE THESE in production!
# JWT_SECRET=change-me-minimum-32-characters-long
# JWT_ACCESS_MINUTES=60
# JWT_REFRESH_DAYS=30

# API key encryption — generate with:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# ENCRYPTION_KEY=

# Flask
# FLASK_ENV=development

# Database
DATABASE_URL=postgresql+psycopg2://webanalyzer:secret@postgres:5432/webanalyzer

# Redis / Celery
REDIS_URL=redis://redis:6379/0

# Flask
FLASK_ENV=production

# JWT secret.
# Generate:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
JWT_SECRET=change-me-in-production

# Fernet encryption key for user API keys.
# Generate:
# python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=change-me